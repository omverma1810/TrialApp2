import { StyleSheet } from 'react-native';

const MynoteStyles = StyleSheet.create({
    notesContainer: {
        gap: 10,
    },
    notesTitle: {
        fontSize: 15,
        color: '#161616',
        fontWeight: '500',
    },
});

export default MynoteStyles;
