const BASE_URL = 'https://dev.piatrika.com/';

const API_CONFIG = {};

  const URL = {
    LOGIN: 'auth/login/',
    REFRESH_TOKEN: 'auth/token/refresh/',
    PROFILE : 'auth/me/',
    LOGOUT: 'auth/logout/'
  };

export {URL, BASE_URL, API_CONFIG};