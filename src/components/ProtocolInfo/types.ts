export interface ProtocolInfoProps {
  stageName?: string;
  dateOfSowing?: string;
  dueDate?: string;
}

export interface ProtocolData {
  stageName: string;
  dateOfSowing: string;
  dueDate: string;
}
