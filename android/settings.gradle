rootProject.name = 'TrialApp2'

// look first in react-native's nested node_modules, then (if you ever install top-level) fall back
def cliPlatformGradle = file("../node_modules/react-native/node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
if (cliPlatformGradle.exists()) {
    apply from: cliPlatformGradle
} else {
    apply from: file("../node_modules/@react-native-community/cli-platform-android/native_modules.gradle")
}

// this wires up auto-linked native modules:
applyNativeModulesSettingsGradle(settings)

include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')